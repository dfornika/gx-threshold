channels:
  - conda-forge
  - bioconda
dependencies:
  # Only needs stdlib (argparse, collections) - pinned to a plain python
  # conda env, same as LIBRARY_TYPE and SELECT_REFERENCE_ACCESSION.
  - conda-forge::python=3.12
