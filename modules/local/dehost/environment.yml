channels:
  - conda-forge
  - bioconda
dependencies:
  # Pinned to match the mulled container (minimap2 2.28 + samtools 1.20, confirmed
  # via the nf-test stub snapshot) so all engines report identical tool versions.
  - bioconda::minimap2=2.28
  - bioconda::samtools=1.20
